# 心跳任务

<!--
本文件由你的 nanobot agent 周期性检查。当 nanobot gateway 以 gateway.heartbeat.enabled=true 启动时，它会自动注册一个受保护的心跳 cron 任务来读取本文件。

如果本文件没有任务（只有标题和注释），agent 会跳过它。已完成的任务应当删除，而不是保留 - 心跳只读取"Active Tasks"。
-->

## Active Tasks

<!-- 在此行下方添加你的周期性任务 -->
